<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
	

<mapper namespace="com.beer.model.dao.SNSDAO" >
	<select id="search" parameterType="int" resultType="sns">
		select *
		from SNS
		where articleId=#{articleId}
	</select>
	
	<select id="allsns" resultType="sns">
		select * from SNS
	</select>

	<select id="searchByUser" parameterType="string" resultType="sns">
		select *
		from SNS
		where userId=#{userId} or (
			select follower
			from Follow
			where following=#{userId}
		)
	</select>
	
	<select id="searchByEvery" parameterType="string" resultType="sns">
		select *
		from SNS
		where userId like concat('%',#{keyword},'%') or contents like concat('%',#{keyword},'%')
	</select>
	
	<insert id="insert" parameterType="sns">
		insert into SNS(articleId, userId, date, picture, contents)
		values(
			#{articleId:INTEGER},
			#{userId:VARCHAR},
			now(),
			#{picture:BLOB},
			#{contents:VARCHAR}
		)
	</insert>
	
	<update id="update" parameterType="sns">
		update SNS set(
			picture=#{picture:BLOB},
			contents=#{contents:VARCHAR}
		)
		where articleId=#{articleId:INTEGER}
	</update>
	
	<delete id="delete" parameterType="int">
		delete from SNS
		where articleId=#{articleId:BLOB}
	</delete>
</mapper>
