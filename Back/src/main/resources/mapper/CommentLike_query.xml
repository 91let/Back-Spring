<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.beer.model.dao.CommentLikeDAO">
	<insert id="insertCommentLike" parameterType="commentlike">
		insert into COMMENT_LIKE(commentId, userId)
		values(
		#{commentId},
		#{userId}
		)
	</insert>
	
	<delete id="deleteCommentLike" parameterType="commentlike">
		delete
		from COMMENT_LIKE
		where userId=#{userId} and commentId=#{commentId}
	</delete>
	
	<select id="likeCommentLike" resultType="int">
		select count(*)
		from COMMENT_LIKE
		where commentId = #{commentId} and userId = #{userId}
	</select>

	<select id="countCommentLike" parameterType="int"
		resultType="int">
		select count(*)
		from COMMENT_LIKE
		where commentId = #{commentId}
	</select>
</mapper>